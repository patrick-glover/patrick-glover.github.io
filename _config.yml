title: Patrick Glover, MD
description: fighting entropy locally
url: https://patrickglover.io
permalink: /writing/:title/
markdown: kramdown

plugins:
  - jekyll-seo-tag
  - jekyll-feed

# Cloudflare Web Analytics site token (dash.cloudflare.com → Analytics & Logs → Web Analytics).
# Leave empty to disable. Also used by the project repos via /assets/analytics.js.
cloudflare_analytics_token: ""
